const nombre = "Noemy";
const app = "la aplicacion";
let usoCompleto = `la frase completa es ${nombre} ${app}`;
console.log(usoCompleto);

let segundos = 29;
console.log(`El sugundo es : ${segundos}`);
